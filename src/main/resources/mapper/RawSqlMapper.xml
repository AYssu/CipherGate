<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ayssu.ciphergate.mapper.RawSqlMapper">
    
    <update id="executeRawSql">
        ${sql}
    </update>
    
    <select id="executeRawQueryForInteger" resultType="java.lang.Integer">
        ${sql}
    </select>
    
    <select id="executeRawQueryForString" resultType="java.lang.String">
        ${sql}
    </select>
    
</mapper>