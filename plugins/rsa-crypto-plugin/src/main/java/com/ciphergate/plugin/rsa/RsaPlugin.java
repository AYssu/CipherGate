package com.ciphergate.plugin.rsa;

import org.pf4j.Plugin;
import org.pf4j.PluginWrapper;

/**
 * RSA 双向加密插件 PF4J 入口。
 */
public class RsaPlugin extends Plugin {

    public RsaPlugin(PluginWrapper wrapper) {
        super(wrapper);
    }
}
