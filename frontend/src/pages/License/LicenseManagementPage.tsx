import React from 'react';
import { LicenseManagementContent } from '../../components';

const LicenseManagementPage: React.FC = () => {
  return <LicenseManagementContent />;
};

export default LicenseManagementPage;
