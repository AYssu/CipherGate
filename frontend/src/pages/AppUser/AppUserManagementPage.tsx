import React from 'react';
import { AppUserManagementContent } from '../../components';

const AppUserManagementPage: React.FC = () => {
  return <AppUserManagementContent />;
};

export default AppUserManagementPage;
